# NightBridge 26.5 Release Notes Draft

NightBridge 26.5 is the first CalVer release candidate for the original
NightBridge problem: a LocalSend-compatible endpoint that can run headless on a
server, NAS, Raspberry Pi, homelab box, or always-on workstation.

The alpha release is centered on receiving from official LocalSend apps without
keeping a desktop app open, plus a Native Mesh LAN preview for trusted
NightBridge servers on the same network. WAN rendezvous, WebUI, and desktop GUI
work are included as advanced paths, but the release story is LocalSend
compatibility plus stricter server-to-server native transfers.

This is a release notes draft until the final 26.5 preflight, demo evidence,
and release retro are complete.

## Pre-Release Scope

NightBridge 26.5 is in pre-release closure. Feature scope is officially closed
for this release train. Until `26.5.0` is tagged, changes should be limited to
bug fixes, release-blocker fixes, security fixes, documentation corrections,
packaging/release evidence improvements, and small enhancements that improve
the existing pre-release experience without adding a new product surface.

## Release Positioning

NightBridge 26.5 should be described as:

> A LocalSend-compatible headless server for homelabs, NAS boxes, and always-on
> machines.

Use this framing for the first release:

- Official LocalSend app on phone or desktop sends to NightBridge.
- NightBridge runs as a daemon, service, container, or server binary.
- Operators control who may upload through trusted LocalSend fingerprints.
- CLI, TUI, and API surfaces exist for headless administration.
- The TUI defaults to LocalSend receiver administration. Native/QUIC details
  stay hidden unless started with `--advanced`.
- Native Mesh LAN preview lets trusted NightBridge daemons discover each other
  and send files over native QUIC by peer alias.
- WAN rendezvous remains advanced and is not the first-release headline.

## What 26.5 Includes

- Headless daemon with LocalSend v2 receive/send compatibility.
- LocalSend-compatible receive policy for `prompt`, `trusted`, and explicit
  `auto` test deployments.
- Pending LocalSend peer store plus CLI/API approval and rejection flows.
- CLI for status, inbox, LocalSend-compatible sends, native sends, WAN lookup,
  and send-by-peer operations. The short alias is `nbrg`.
- HTTP/gRPC daemon API protected by bearer tokens.
- TUI LocalSend receiver surface for terminal administration, including pending
  sender selection plus approve/deny actions.
- WebUI status/operations surface.
- systemd unit, Docker image path, DEB packaging path, release binaries, SBOM,
  and checksum scripts.
- Native QUIC + TLS 1.3 transfer path with Ed25519 identity material and trusted
  peer certificate fingerprint verification.
- Native Mesh LAN preview with native peer discovery, approval, and
  `night-bridge send --native --peer <alias-or-fingerprint>`.
- Self-hosted WAN rendezvous control plane for native direct-path discovery.
- Tauri desktop GUI scaffold with remote daemon mode and standalone local daemon
  mode.
- Hook delivery, metrics, and health/readiness endpoints.
- Operator, user, deployment, security, interop, and release documentation.

## Expected Install Artifacts

Required 26.5 release artifacts are the headless-server artifacts:

- Linux `night-bridge` and `night-bridge-daemon`
- macOS `night-bridge` and `night-bridge-daemon`
- Windows `night-bridge.exe` and `night-bridge-daemon.exe`
- Docker image
- Debian package with systemd unit
- SBOM and checksum files

Optional or pre-release artifacts include:

- `night-bridge-tui` on targets where terminal UI validation is complete
- macOS desktop GUI bundle or DMG
- Windows desktop installer
- Linux desktop bundle
- RPM daemon package

See [release artifacts](artifacts.md) for the artifact matrix and validation
status.

## LocalSend Compatibility

LocalSend v2 compatibility remains a first-class boundary. The daemon can
receive from official LocalSend peers and the CLI/daemon can send to compatible
LAN peers through the LocalSend v2 path.

The LocalSend compatibility path intentionally preserves official-app
self-signed TLS behavior where required. That exception is isolated from the
native protocol and must not be reused by native sends.

Current interop status is tracked in the
[LocalSend interop matrix](../interop/localsend-matrix.md).

## Native Protocol

The native protocol uses QUIC and TLS 1.3 with Ed25519 peer identity material.
Sprint 7 hardens the native trust boundary by binding native sends to trusted
peer certificate fingerprints.

Native production paths now reject:

- unknown peers
- blocked peers
- missing native certificate metadata
- mismatched native certificate fingerprints

Direct native URL sends require explicit certificate fingerprint material when
trusted peer metadata is not available.

## Native Mesh LAN Preview Evidence

Manual LAN validation passed on 2026-05-19 against two Ubuntu hosts on
`10.16.20.0/24`:

- Docker node: `link`, `10.16.20.130`, alias `NightBridge Docker link`.
- systemd node: `zelda`, `10.16.20.129`, alias `NightBridge systemd zelda`.
- `peers list-native` discovered each daemon from the other side without a
  manual transfer IP.
- `peers approve-native` stored trusted `auto_accept` records with pinned
  native certificate metadata in both daemon trust stores.
- Docker to systemd send by alias:
  `8ce688c7-4975-4e03-bea5-50b92786734a`, checksum
  `1a0600e08ce4279050b338dc3e9e46ec05d87cff543ae312f0e6801fb3d6b2fb`.
- systemd to Docker send by alias:
  `742d5a93-d01f-4962-860c-1ddacbdab55e`, checksum
  `1f2d7a875273946b29491b18a06bb240f91425cc4496829fa4fdfc6abc0e9729`.

The validation also confirmed the operational firewall requirement: mDNS
discovery uses UDP `5353`, but native mesh transfers require the configured
native QUIC UDP port, `53400` by default, to be allowed between trusted LAN
nodes.

## WAN Rendezvous

WAN rendezvous is self-hosted and control-plane only. It lets daemons register
and look up native QUIC candidates, then peers attempt a direct native
connection.

Rendezvous does not relay file bytes and there is no default public rendezvous
service. Operators choose their own rendezvous server, logging policy, and
retention boundary.

Known WAN limitations remain:

- symmetric NAT can block direct connectivity
- carrier-grade NAT can block direct connectivity
- strict firewalls and UDP-blocking networks can prevent native QUIC transfer
- no relay fallback is included in the open-base 26.5 path

## GUI Status

The desktop GUI is a Tauri 2 app that bundles the existing Svelte WebUI. It has
two modes:

- remote daemon mode for connecting to an existing daemon endpoint
- standalone local daemon mode for starting and managing a local daemon process

The GUI can surface WAN direct-path diagnostics from daemon transfer events.
Local macOS packaging has produced a DMG, but platform release status remains
pre-release until signing, notarization, updater, Windows, and Linux validation
are complete.

## Security Posture

The intended 26.5 security boundary is the daemon and protocol crates:

- daemon API requests require bearer-token authentication
- native sends require trusted peer policy and pinned certificate identity
- LocalSend v2 self-signed TLS compatibility is scoped to the compatibility path
- LocalSend-compatible receive defaults to prompt/reject and requires trusted
  fingerprint approval through the daemon API/CLI, config/file allowlisting, or
  explicit auto-accept for compatibility tests
- rendezvous cannot authorize transfer data or relay file bytes
- WebUI token bootstrap does not expose an unauthenticated token endpoint

Operators should read the [threat model](../security/threat-model.md),
[native trust audit](../security/native-trust-audit.md), and
[rendezvous privacy model](../security/rendezvous-privacy.md) before exposing
services outside localhost or a trusted LAN.

## Audit Status

The project has a security policy and an audit invite packet, but no completed
third-party audit yet. The current audit-ready packet calls out native trust,
daemon API authentication, LocalSend compatibility boundaries, rendezvous
privacy, release artifacts, and GUI token handling. NightBridge is open source
and welcomes public review, but that is not a substitute for a funded external
audit.

See [security audit invite](../security/audit-invite.md).

## Release Verification Evidence

Preflight command:

```bash
bash scripts/preflight-26.5.sh
```

Evidence captured:

- Date: 2026-05-18 23:07:17 CDT
- Branch: `feature/nightbridge-rename`
- Base commit before commit: `2d410d7`
- Result: PASS outside the Codex sandbox after LocalSend LAN discovery,
  Native Mesh LAN preview, and native mDNS advertise-address fixes

The first sandboxed run from earlier validation failed during
`cargo test --workspace` because the sandbox blocked a local listener with
`Operation not permitted`. The preflight should run outside the sandbox.

The first 2026-05-18 run outside the sandbox exposed a host limit issue:
`cargo test --workspace` failed one QUIC listener test with
`Too many open files (os error 24)` under the default macOS soft limit of 256
file descriptors. The preflight now raises the soft file descriptor limit to
4096 when supported, and the full preflight passed after that adjustment.

Covered checks:

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `npm run check --prefix crates/webui`
- `npm run build --prefix crates/webui`
- `cargo test -p lsi-protocol-native-v1 --test soak_resume patterned_bytes_have_stable_hash`
- `cargo test --test gui_smoke -- --nocapture`
- `bash scripts/localsend-interop-smoke.sh`

Final 26.5 release evidence to attach after soak:

- Release commit: pending final commit; current checkout is based on `e10ade9`
  plus release evidence updates and a `socket2` release-blocker fix required by
  final preflight
- Final preflight:
  - Command: `bash scripts/preflight-26.5.sh`
  - Host: `erebor`
  - Start: `2026-05-28T05:46:59Z`
  - Finish: `2026-05-28T05:47:08Z`
  - Result: PASS
  - Evidence log: `target/release-evidence/preflight-26.5.log`
- Native soak:
  - Host: `camelia` (`10.16.20.128`)
  - Session: `nightbridge-soak-26_5`
  - Path: `/home/serveradmin/nightbridge-release/d46c64a`
  - Start: `2026-05-17T23:41:41Z`
  - Finish: `2026-05-24T23:41:51Z`
  - Result: PASS; final sampled logs ended with `test result: ok. 1 passed; 0 failed`
  - Completed runs: `13591`
  - Evidence path: `target/soak/evidence/`
- Delta native soak after security refresh:
  - Reason: dependency security refresh changed the release binary after the
    long soak had already started
  - Commit: `e10ade9`
  - Host: `camelia` (`10.16.20.128`)
  - Session: `nightbridge-delta-soak-e10ade9`
  - Path: `/home/serveradmin/nightbridge-release/e10ade9`
  - Duration target: 12 hours
  - Start: `2026-05-27T17:35:58Z`
  - Finish: `2026-05-28T05:36:16Z`
  - Result: PASS; final log ended with `test result: ok. 1 passed; 0 failed`
  - Completed runs: `928`
  - Evidence path: `target/soak/evidence/`
- Release artifacts:
  - Clean `dist/` path: `TBD`
  - Artifact list: `TBD`
  - GitHub release tarballs: `TBD`
  - `SHA256SUMS`: `TBD`
  - `sbom.cdx.json`: `TBD`
  - Result: `TBD`
- Docker smoke:
  - Host: `link` (`10.16.20.130`)
  - Image tag: `night-bridge:26.5.0`
  - Image ID: `TBD`
  - Command: `bash packaging/docker/smoke.sh`
  - Result: `TBD`
  - Evidence log: `TBD`
- Debian package and systemd smoke:
  - Host: `zelda` (`10.16.20.129`)
  - Package: `target/debian/night-bridge-daemon_26.5.0-1_amd64.deb`
  - Command: `bash packaging/build-packages.sh --deb-only` plus
    install/systemd smoke
  - Service status: `TBD`
  - `/healthz`: `TBD`
  - Evidence logs: `TBD`

Skipped or gated release coverage:

- GUI smoke was gated because `NBRG_RUN_GUI_SMOKE=1` was not set.
- Official LocalSend app matrix remains manual unless
  `NBRG_OFFICIAL_LOCALSEND_ARTIFACT` points to a verified headless-capable
  artifact for this platform. Manual bidirectional testing has passed against
  Android, iOS, Windows, macOS, and Linux official apps with trusted policy,
  pending-peer approval, and daemon-to-app sends by discovered LAN peer alias.
- Docker image smoke passed on Ubuntu host `link` for commit `1d18b86`.
- Debian package generation and systemd service smoke passed on Ubuntu host
  `zelda` for commit `1d18b86`; the service was stopped after validation.
- Non-strict GUI smoke passed on the local macOS workstation; strict WebDriver
  coverage remains scoped out because `tauri-driver` is not installed.
- Release checksum and SBOM scripts passed against a temporary local `dist`
  smoke directory; final release artifacts must be regenerated after soak.
- APT/PPA distribution, RPM packaging, Homebrew, production desktop signing,
  notarization, updater publishing, and strict GUI WebDriver coverage are
  scoped out for 26.5 unless explicit credentials and tooling are added before
  tagging.
- The real 7-day soak and the post-refresh delta soak both completed on
  `camelia`.

## Known Limitations

- Final release artifacts must be regenerated and smoked from the release
  commit before tagging.
- APT/PPA and Homebrew distribution are deferred; GitHub Releases, direct `.deb`
  download, and the checksum-verifying curl installer are the 26.5 distribution
  path.
- Desktop packages are unsigned pre-release artifacts until platform signing
  and notarization are configured.
- Windows and Linux desktop bundles need native host validation.
- Strict GUI WebDriver smoke requires `tauri-driver` and an
  interactive-capable host; non-strict GUI smoke may run on the local macOS
  workstation.
- WAN has no relay fallback.
- Rendezvous registry state is in-memory.
- Existing trusted peers without native certificate metadata need refresh or
  re-pairing before native WAN or Native Mesh auto-send can work.
- Generated final `dist` release artifacts, checksums, and SBOM still need
  validation on the release commit before broad production distribution.
